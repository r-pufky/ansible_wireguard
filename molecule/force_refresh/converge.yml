---
- name: 'Converge'
  hosts: 'all'
  gather_facts: true  # ansible_facts['kernel'].
  tasks:
    - name: 'Converge | role | r_pufky.deb.wireguard'
      ansible.builtin.include_role:
        name: 'r_pufky.deb.wireguard'
